package com.platforms.leaderboard.configservice.constants

/**
 * Represents the data type of a feature in the leaderboard configuration
 */
enum class FeatureDataType {
    STRING,
    INTEGER,
    FLOAT,
    BOOLEAN
}
